import { SiYourtraveldottv } from "react-icons/si";
import styled from "styled-components";

export const StyledPacman = styled(SiYourtraveldottv)`
  font-size: 3rem;
  color: ${({ theme }) => theme.colour.secondary};;
`
